# Ragers Engine — Certification Evidence Ledger

**Classification:** Internal. Generated by `npm run certify` in `engine/`.
**Generated:** 2026-09-09T00:45:11.205Z

## Certification status: `RAGERS_ENGINE_E2E_READY_WITH_EXTERNAL_BLOCKERS`

13 gate(s) passed, 0 failed, 5 blocked by an external dependency.

| | Gate | Required gate | Result |
|---|---|---|---|
| ✅ | Schema & migrations (static) | schema/migrations | 15/15 assertions passed (0.2s) |
| ✅ | Unit tests | unit | 130/130 assertions passed (1.0s) |
| ✅ | Integration tests | integration | 82/82 assertions passed (1.3s) |
| ✅ | Authorization | authorization | 27/27 assertions passed (0.4s) |
| ✅ | Retry & dead-letter | retry/dead-letter | 7/7 assertions passed (0.2s) |
| ✅ | Concurrency & idempotency | concurrency | 19/19 assertions passed (0.4s) |
| ✅ | Voice capture & golden path | voice | 21/21 assertions passed (0.3s) |
| ✅ | Moderation failure path (fail-closed) | moderation failure-path | 6/6 assertions passed (0.3s) |
| ✅ | Privacy & search leakage | privacy/search leakage | 39/39 assertions passed (0.7s) |
| ✅ | Accessibility (static surfaces) | accessibility | 13/13 assertions passed (0.2s) |
| ✅ | Strict typecheck | build | clean (1.6s) |
| ✅ | Production build | build | clean (14.4s) |
| ✅ | Browser E2E | browser E2E | 10 browser test(s) passed (8.5s) |
| ⛔ | Migrations applied to a live database | schema/migrations | No Postgres/Supabase project is provisioned for this session, so the migrations in supabase/migrations are verified statically but never applied. |
| ⛔ | RLS enforcement against a live database | authorization | RLS policies can only be executed against a live Postgres instance. The application policy layer they mirror is covered by the authorization gate. |
| ⛔ | Deployment to a target environment | deployment | No deployment target (Vercel/Railway project) is configured for this session. |
| ⛔ | Backup & restore drill | backup/restore | Requires a live database to back up and restore. Procedure is documented in docs/OPERATIONS.md. |
| ⛔ | Rollback drill | rollback | Requires a deployed environment to roll back. Procedure is documented in docs/OPERATIONS.md. |

## External blockers

These gates are not runnable in this environment. They are named rather than skipped, and they are why the status is not a plain `RAGERS_ENGINE_E2E_READY`.

- **Migrations applied to a live database** — No Postgres/Supabase project is provisioned for this session, so the migrations in supabase/migrations are verified statically but never applied.
- **RLS enforcement against a live database** — RLS policies can only be executed against a live Postgres instance. The application policy layer they mirror is covered by the authorization gate.
- **Deployment to a target environment** — No deployment target (Vercel/Railway project) is configured for this session.
- **Backup & restore drill** — Requires a live database to back up and restore. Procedure is documented in docs/OPERATIONS.md.
- **Rollback drill** — Requires a deployed environment to roll back. Procedure is documented in docs/OPERATIONS.md.

## Reproducing this ledger

```bash
cd engine
npm install
npm run certify
```
